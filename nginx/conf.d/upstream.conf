# Define upstream groups for stable and canary servers

upstream app_stable {
    server stable1:8080;   # Main production server
    keepalive 100;
}

upstream app_canary {
    server canary1:8080;   # Canary test server (10% traffic)
    keepalive 50;
}
